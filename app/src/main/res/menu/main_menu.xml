<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/homeFragment"
        android:icon="@drawable/home"
        android:title="@string/home_label"
        app:showAsAction="ifRoom"
        />

    <item
        android:id="@+id/estimationFragment"
        android:title="@string/estimation_label"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/budgetingFragment"
        android:title="@string/budgeting_label"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/upcomingFragment"
        android:title="@string/upcoming_label"
        app:showAsAction="never"
        />

</menu>