<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingLeft="16dp"
    android:paddingTop="10dp"
    android:paddingRight="16dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/budgetingNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="NAME_PLACEHOLDER" />

    <TextView
        android:id="@+id/budgetingCategoryTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintStart_toStartOf="@+id/budgetingNameTextView"
        app:layout_constraintTop_toBottomOf="@+id/budgetingNameTextView"
        tools:text="CATEGORY_PLACEHOLDER" />

    <TextView
        android:id="@+id/budgetingValueTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="15dp"
        android:textSize="16sp"
        app:layout_constraintEnd_toStartOf="@+id/budgetingDeleteButton"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="VALUE_PH" />

    <TextView
        android:id="@+id/budgetingTypeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintEnd_toEndOf="@+id/budgetingValueTextView"
        app:layout_constraintTop_toBottomOf="@+id/budgetingValueTextView"
        tools:text="TYPE_PH" />

    <Button
        android:id="@+id/budgetingDeleteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/delete_label"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/budgetingUpdateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/update_label"
        app:layout_constraintEnd_toEndOf="@+id/budgetingDeleteButton"
        app:layout_constraintTop_toBottomOf="@+id/budgetingDeleteButton" />

</androidx.constraintlayout.widget.ConstraintLayout>