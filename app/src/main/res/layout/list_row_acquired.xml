<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="16dp"
    android:paddingTop="10dp"
    android:paddingRight="16dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/acquiredNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="NAME_PLACEHOLDER" />

    <Button
        android:id="@+id/acquiredDeleteButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/delete_label"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/acquiredValueTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="15dp"
        android:textSize="16sp"
        app:layout_constraintEnd_toStartOf="@+id/acquiredDeleteButton"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="VALUE_PH" />

    <TextView
        android:id="@+id/acquiredCategoryTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintStart_toStartOf="@+id/acquiredNameTextView"
        app:layout_constraintTop_toBottomOf="@+id/acquiredNameTextView"
        tools:text="CATEGORY_PLACEHOLDER" />

    <Button
        android:id="@+id/acquiredUpdateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/update_label"
        app:layout_constraintEnd_toEndOf="@+id/acquiredDeleteButton"
        app:layout_constraintTop_toBottomOf="@+id/acquiredDeleteButton" />

    <TextView
        android:id="@+id/acquiredTypeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        app:layout_constraintEnd_toEndOf="@+id/acquiredValueTextView"
        app:layout_constraintTop_toBottomOf="@+id/acquiredValueTextView"
        tools:text="TYPE_PH"/>

</androidx.constraintlayout.widget.ConstraintLayout>